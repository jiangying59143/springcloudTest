spring:
  application:
    name: configclient
  cloud:
    config:
      discovery:
        service-id: remoteConfigureServer
        enabled: true
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
